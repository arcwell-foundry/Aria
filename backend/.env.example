# ──────────────────────────────────────────────────────────────────────
# ARIA Backend — Environment Variables
# Copy to .env and fill in values. All secrets are REQUIRED unless noted.
# ──────────────────────────────────────────────────────────────────────

# ── REQUIRED — App will not start without these ─────────────────────
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
ANTHROPIC_API_KEY=sk-ant-your-api-key
APP_SECRET_KEY=your-secret-key-at-least-32-characters-long

# OpenAI (required for Graphiti/Neo4j embeddings)
OPENAI_API_KEY=your-openai-api-key

# ── NEO4J / GRAPHITI — Required for knowledge graph ────────────────
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=your-neo4j-password

# ── TAVUS — Avatar & video (Phase 6) ───────────────────────────────
# TAVUS_API_KEY=your-tavus-api-key
# TAVUS_PERSONA_ID=your-persona-id
# TAVUS_REPLICA_ID=your-replica-id
# TAVUS_CALLBACK_URL=https://aria-api.onrender.com/api/v1/webhooks/tavus
# TAVUS_GUARDRAILS_ID=
# TAVUS_WEBHOOK_SECRET=

# ── DAILY.CO — WebRTC transport for avatar ──────────────────────────
# DAILY_API_KEY=your-daily-api-key

# ── COMPOSIO — OAuth integrations (CRM, email, calendar) ───────────
# COMPOSIO_API_KEY=your-composio-api-key
# COMPOSIO_BASE_URL=https://api.composio.dev

# ── EXA — Web search & enrichment ──────────────────────────────────
# EXA_API_KEY=your-exa-api-key
# EXA_WEBHOOK_SECRET=

# ── STRIPE — Billing & payments (US-928) ───────────────────────────
# STRIPE_SECRET_KEY=your-stripe-secret-key
# STRIPE_WEBHOOK_SECRET=your-stripe-webhook-secret
# STRIPE_PRICE_ID=your-stripe-price-id

# ── RESEND — Email delivery (US-934) ───────────────────────────────
# RESEND_API_KEY=your-resend-api-key
# FROM_EMAIL=ARIA <aria@luminone.com>

# ── APPLICATION SETTINGS ────────────────────────────────────────────
APP_ENV=development
APP_URL=http://localhost:5173

# CORS — comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Rate limiting
RATE_LIMIT_ENABLED=true
RATE_LIMIT_REQUESTS_PER_MINUTE=100

# Background scheduler (set to "false" to disable)
ENABLE_SCHEDULER=true

# Logging format ("text" for dev, "json" for production)
# LOG_FORMAT=text

# ── SKILLS.SH — Optional skill marketplace integration ─────────────
# SKILLS_SH_API_URL=https://api.skills.sh/v1
# SKILLS_SYNC_INTERVAL_HOURS=24
